{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for VINote.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
